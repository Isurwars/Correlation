// Correlation - Liquid and Amorphous Solid Analysis Tool
// Copyright © 2013-2026 Isaías Rodríguez (isurwars@gmail.com)
// SPDX-License-Identifier: MIT
// Full license: https://github.com/Isurwars/Correlation/blob/main/LICENSE

import {
    MaterialStyleMetrics,
    MaterialTypography,
    CheckBox,
    CheckState,
    TextField,
    DropDownMenu,
    FilledCard,
    MaterialText,
    Icon,
    Icons,
    MaterialPalette,
    ScrollView,
    ExpandableHeader,
    CheckBoxRow,
} from "@material";
import { BondCutoff, CalculatorInfo, CalculatorGroup } from "Types.slint";

// ============================================================================
// OptionsCard Component
// ============================================================================
// This component encapsulates the configuration inputs for the analysis (e.g.,
// RDF max r, S(Q) max Q, smoothing options, and bond cutoffs).
// 
// C++ Binding Architecture:
// - The properties defined here (like `r_max`, `smoothing_sigma`) are bound
//   bi-directionally to the `AppWindow` root component.
// - The C++ controller (`AppController::handleOptionsfromUI`) reads these values
//   through the `AppWindow` instance when an analysis is started, parsing the 
//   strings into appropriate numeric types for the backend (`AppBackend`).
// - Conversely, when a file is loaded, the backend recommendations are pushed
//   back to the UI (via `AppController::handleOptionstoUI`), updating these
//   same properties to display default values.
// ============================================================================
export component OptionsCard inherits FilledCard {
    in-out property <string> r_max;
    in-out property <string> r_bin_width;
    in-out property <string> q_max;
    in-out property <string> q_bin_width;
    in-out property <string> r_int_max;
    in-out property <string> angle_bin_width;
    in-out property <string> dihedral_bin_width;
    in-out property <string> max_ring_size;
    in-out property <bool> smoothing: true;
    in-out property <bool> use_hdf5: true;
    in-out property <bool> use_csv: true;
    in-out property <string> smoothing_sigma;
    in-out property <int> smoothing_kernel;
    in-out property <[BondCutoff]> bond_cutoffs;
    in property <bool> file_loaded;
    // Dynamic calculator groups from CalculatorFactory via C++
    in-out property <[CalculatorGroup]> calculator_groups: [];
    callback toggle_calculator(string, bool);
    in property <int> total_calculator_count: 0;

    // Toggle stats for groups
    in-out property <bool> analyses_expanded: false;
    in-out property <bool> rdf_expanded: false;
    in-out property <bool> sq_expanded: false;
    in-out property <bool> angle_expanded: false;
    in-out property <bool> md_expanded: false;
    in-out property <bool> smoothing_expanded: false;
    in-out property <bool> bond_cutoffs_expanded: false;

    // Toggle height for groups
    property <length> analyses_height: analyses_expanded ? (root.total_calculator_count * 28px + root.calculator_groups.length * 14px + 32px) : 0px;
    property <length> rdf_height: rdf_expanded ? 100px : 0px;
    property <length> sq_height: sq_expanded ? 150px : 0px;
    property <length> angle_height: angle_expanded ? 100px : 0px;
    property <length> md_height: md_expanded ? 50px : 0px;
    property <length> smoothing_height: smoothing_expanded ? 100px : 0px;
    property <length> bond_cutoffs_height: bond_cutoffs_expanded ? min(150px, root.bond_cutoffs.length * 50px) : 0px;

    // Hover states for tooltips
    property <string> hovered_calculator_description: "";
    out property <string> hovered_tooltip_text:
        r_max_area.has-hover ? "Maximum radius to calculate Radial Distribution Functions (g(r), J(r), G(r)). The recommended max radius is 20 Å." :
        r_bin_area.has-hover ? "Bin width used to calculate the histograms for the Radial Distribution Functions (g(r), J(r), G(r)). The recommended bin is 0.02 Å." :
        q_max_area.has-hover ? "Maximum scattering vector (Q) to calculate the Structure Factor (S(Q)). The recommended max scattering vector is 20.0 Å⁻¹" :
        q_bin_area.has-hover ? "Bin width used to calculate the histograms for the Structure Factor. The recommended bin is 0.02 Å⁻¹." :
        r_int_area.has-hover ? "Maximum radius for the Fourier Transform of the g(r) used to calculate the Structure Factor. The recommended max radius is 10.0 Å" :
        theta_bin_area.has-hover ? "Bin width used to calculate the histograms for the Plane-Angle Distribution. The recommended bin is 1.0°." :
        dihedral_bin_area.has-hover ? "Bin width used to calculate the histograms for the Dihedral-Angle Distribution. The recommended bin is 1.0°." :
        ring_size_area.has-hover ? "Maximum ring size to search for in topological motif analysis." :
        sigma_area.has-hover ? "Sigma to be used for the smoothing of the distribution functions. The recommended  sigma is 0.01." :
        kernel_area.has-hover ? "Kernel smoother type.\nGaussian: General purpose smoothing.\nBump: Sharp cutoff, preserves high frequencies.\nTriweight: Higher weight near the center, good for noisy data." :
        root.hovered_calculator_description != "" ? root.hovered_calculator_description :
        "";

    VerticalLayout {
        padding: MaterialStyleMetrics.spacing_4;
        spacing: MaterialStyleMetrics.spacing_4;
        alignment: space-around;
        
        // 0. Select Analyses - dynamic list from CalculatorFactory
        ExpandableHeader {
            text: "Select Analyses";
            expanded <=> root.analyses_expanded;
        }

        VerticalLayout {
            alignment: start;
            height: root.analyses_height;
            opacity: root.analyses_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_4;
            padding_left: MaterialStyleMetrics.spacing_8;

            for group[gi] in root.calculator_groups: VerticalLayout {
                spacing: MaterialStyleMetrics.spacing_2;
                if gi > 0: Rectangle {
                    height: 1px;
                    background: MaterialPalette.outline_variant;
                }

                MaterialText {
                    text: group.name;
                    style: MaterialTypography.label_small;
                    color: MaterialPalette.on_surface_variant;
                }

                for calc[ci] in group.calculators: HorizontalLayout {
                    height: 28px;
                    padding-left: MaterialStyleMetrics.spacing_2;

                    calc_touch := TouchArea {
                        property <bool> _hover: self.has-hover;
                        changed _hover => {
                            if (self.has-hover) {
                                root.hovered_calculator_description = calc.description;
                            } else {
                                root.hovered_calculator_description = "";
                            }
                        }

                        CheckBoxRow {
                            enabled: root.file_loaded;
                            check_state_bool: calc.enabled;
                            text: calc.name;
                            horizontal-stretch: 1;
                            checked_state_changed(s) => {
                                root.toggle_calculator(calc.id, s);
                            }
                        }
                    }
                }
            }
        }

        // 1. Radial Distribution Group
        ExpandableHeader {
            text: "Radial Distribution";
            expanded <=> root.rdf_expanded;
        }

        VerticalLayout {
            height: root.rdf_height;
            opacity: root.rdf_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8; // Indent content
            r_max_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "RDF Max r (Å)";
                    text <=> root.r_max;
                }
            }

            r_bin_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "RDF Bin Width (Å)";
                    text <=> root.r_bin_width;
                }
            }
        }

        // 2. Structure Factor Group
        ExpandableHeader {
            text: "Structure Factor";
            expanded <=> root.sq_expanded;
        }

        VerticalLayout {
            height: root.sq_height;
            opacity: root.sq_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8;
            q_max_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "Max Q (Å⁻¹)";
                    text <=> root.q_max;
                }
            }

            q_bin_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "S(Q) Bin Width (Å⁻¹)";
                    text <=> root.q_bin_width;
                }
            }

            r_int_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "Max r for the FFT (Å)";
                    text <=> root.r_int_max;
                }
            }
        }

        // 3. Bond Angle Group
        ExpandableHeader {
            text: "Bond Angle";
            expanded <=> root.angle_expanded;
        }

        VerticalLayout {
            height: root.angle_height;
            opacity: root.angle_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8;
            theta_bin_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "PAD Bin Width (°)";
                    text <=> root.angle_bin_width;
                }
            }

            dihedral_bin_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "DAD Bin Width (°)";
                    text <=> root.dihedral_bin_width;
                }
            }
        }

        // 3.5 Topological Rings Group
        ExpandableHeader {
            text: "Topological Rings";
            expanded <=> root.md_expanded;
        }

        VerticalLayout {
            height: root.md_height;
            opacity: root.md_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8;
            ring_size_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "Max Ring Size";
                    text <=> root.max_ring_size;
                }
            }
        }

        // 4. Smoothing Group
        ExpandableHeader {
            text: "Smoothing";
            expanded <=> root.smoothing_expanded;
        }

        VerticalLayout {
            height: root.smoothing_height;
            opacity: root.smoothing_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8;
            sigma_area := TouchArea {
                TextField {
                    width: 90%;
                    label: "Smoothing Sigma";
                    text <=> root.smoothing_sigma;
                }
            }

            kernel_area := TouchArea {
                DropDownMenu {
                    width: 90%;
                    label: "Smoothing Kernel";
                    items: [
                        { text: "Gaussian", enabled: true },
                        { text: "Bump", enabled: true },
                        { text: "Triweight", enabled: true }
                    ];
                    current-index <=> root.smoothing_kernel;
                }
            }
        }

        // 5. Bond Cutoffs Group
        ExpandableHeader {
            text: "Bond Cutoffs";
            expanded <=> root.bond_cutoffs_expanded;
        }

        VerticalLayout {
            height: root.bond_cutoffs_height;
            opacity: root.bond_cutoffs_expanded ? 1.0 : 0.0;
            animate height {
                duration: 250ms;
                easing: ease-in-out;
            }
            animate opacity {
                duration: 250ms;
                easing: ease-in-out;
            }
            spacing: MaterialStyleMetrics.spacing_8;
            padding_left: MaterialStyleMetrics.spacing_8;

            ScrollView {
                vertical-stretch: 0;
                height: min(list_layout.preferred-height, root.bond_cutoffs_height);
                viewport-height: list_layout.preferred-height;
                viewport-width: list_layout.preferred-width;
                list_layout := VerticalLayout {
                    spacing: MaterialStyleMetrics.spacing_4;
                    padding: MaterialStyleMetrics.padding_4;
                    for cutoff[i] in root.bond_cutoffs: HorizontalLayout {
                        alignment: center;
                        spacing: MaterialStyleMetrics.spacing_4;
                        MaterialText {
                            min-width: 75px;
                            text: cutoff.element1 + " - " + cutoff.element2 + ":";
                            vertical-alignment: center;
                            horizontal-stretch: 1;
                        }

                        TextField {
                            min-width: 95px;
                            text: cutoff.distance;
                            label: "Cutoff (Å)";
                            edited(s) => {
                                root.bond_cutoffs[i].distance = s;
                            }
                        }
                    }
                }
            }
        }
    }
}
