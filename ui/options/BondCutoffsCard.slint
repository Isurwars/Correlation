import {
    MaterialStyleMetrics,
    MaterialTypography,
    MaterialPalette,
    FilledCard,
    ExpandableHeader,
    TextField,
    MaterialText,
    Vertical,
    Horizontal,
    Matrix,
    TabItem,
} from "../material/material.slint";
import { BondCutoff, AtomCount } from "../Types.slint";

export component BondCutoffsCard inherits FilledCard {
    in-out property <bool> expanded: true;
    in property <bool> file_loaded;
    in property <[AtomCount]> atom_counts;
    in-out property <[BondCutoff]> bond_cutoffs;
    in property <int> bond_cutoffs_reset_trigger: 0;
    callback reset_bond_cutoffs();

    pure function get_flat_index(i: int, j: int, N: int) -> int {
        let r = min(i, j);
        let c = max(i, j);
        let r_term = (r == 0) ? 0 : 
                     (r == 1) ? 1 : 
                     (r == 2) ? 3 : 
                     (r == 3) ? 6 : 
                     (r == 4) ? 10 : 
                     (r == 5) ? 15 : 
                     (r == 6) ? 21 : 
                     (r == 7) ? 28 : 
                     (r == 8) ? 36 : 
                     (r == 9) ? 45 : 
                     (r == 10) ? 55 : 
                     (r == 11) ? 66 : 
                     (r == 12) ? 78 : 
                     (r == 13) ? 91 : 
                     (r == 14) ? 105 : 120;
        return r * N - r_term + c;
    }

    pure function pair_has_error(r: int, c: int) -> bool {
        let idx = root.get_flat_index(min(r, c), max(r, c), root.atom_counts.length);
        if (idx < root.bond_cutoffs.length) {
            let min_val = root.bond_cutoffs[idx].min_distance.to-float();
            let max_val = root.bond_cutoffs[idx].max_distance.to-float();
            return min_val >= max_val || min_val < 0.0 || max_val <= 0.0;
        }
        return false;
    }

    Vertical {

        ExpandableHeader {
            text: "Bond Cutoffs";
            expanded <=> root.expanded;
        }

        Vertical {
            height: root.expanded ? self.preferred-height : 0px;
            visible: self.height > 0px;
            opacity: root.expanded ? 1.0 : 0.0;
            padding_left: MaterialStyleMetrics.spacing_8;
            padding_right: MaterialStyleMetrics.spacing_8;
            padding_bottom: MaterialStyleMetrics.spacing_8;
            spacing: MaterialStyleMetrics.spacing_8;
            animate height, opacity {
                duration: 250ms;
                easing: ease-in-out;
            }

            if (!root.file_loaded): MaterialText {
                text: "No file loaded.";
                color: MaterialPalette.on_surface_variant;
            }

            if (root.file_loaded): HorizontalLayout {
                alignment: space-between;
                spacing: 8px;

                // Legend
                HorizontalLayout {
                    spacing: 12px;
                    alignment: start;

                    // Upper / Max badge
                    HorizontalLayout {
                        spacing: 4px;
                        alignment: center;
                        Rectangle {
                            width: 10px;
                            height: 10px;
                            border-radius: 2px;
                            background: MaterialPalette.primary;
                        }
                        MaterialText {
                            text: "▲ Upper: Max (Å)";
                            style: MaterialTypography.label_small;
                            font_size: 11px;
                            color: MaterialPalette.on_surface_variant;
                        }
                    }

                    // Lower / Min badge
                    HorizontalLayout {
                        spacing: 4px;
                        alignment: center;
                        Rectangle {
                            width: 10px;
                            height: 10px;
                            border-radius: 2px;
                            background: MaterialPalette.tertiary;
                        }
                        MaterialText {
                            text: "▼ Lower: Min (Å)";
                            style: MaterialTypography.label_small;
                            font_size: 11px;
                            color: MaterialPalette.on_surface_variant;
                        }
                    }
                }

                // Reset Defaults action
                TouchArea {
                    width: reset_text.preferred-width + 12px;
                    height: 22px;
                    clicked => {
                        root.reset_bond_cutoffs();
                    }

                    Rectangle {
                        width: 100%;
                        height: 100%;
                        background: parent.has-hover ? MaterialPalette.surface_container_highest : MaterialPalette.surface_container_low;
                        border-radius: 4px;
                        border-width: 1px;
                        border-color: MaterialPalette.outline_variant.with_alpha(50%);

                        reset_text := MaterialText {
                            text: "Reset Defaults";
                            style: MaterialTypography.label_small;
                            font_size: 10px;
                            font_weight: MaterialTypography.medium;
                            color: MaterialPalette.primary;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                        }
                    }
                }
            }

            if (root.file_loaded): Matrix {
                headers: root.atom_counts;
                enabled: root.file_loaded;
                reset_trigger: root.bond_cutoffs_reset_trigger;

                cell_value(r, c) => {
                    let r_idx = min(r, c);
                    let c_idx = max(r, c);
                    let idx = root.get_flat_index(r_idx, c_idx, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        return (c > r) ? root.bond_cutoffs[idx].max_distance : root.bond_cutoffs[idx].min_distance;
                    }
                    return "";
                }

                diagonal_max_value(i) => {
                    let idx = root.get_flat_index(i, i, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        return root.bond_cutoffs[idx].max_distance;
                    }
                    return "";
                }

                diagonal_min_value(i) => {
                    let idx = root.get_flat_index(i, i, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        return root.bond_cutoffs[idx].min_distance;
                    }
                    return "";
                }

                cell_has_error(r, c) => {
                    return root.pair_has_error(r, c);
                }

                cell_edited(r, c, val) => {
                    let r_idx = min(r, c);
                    let c_idx = max(r, c);
                    let idx = root.get_flat_index(r_idx, c_idx, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        if (c > r) {
                            root.bond_cutoffs[idx].max_distance = val;
                        } else {
                            root.bond_cutoffs[idx].min_distance = val;
                        }
                    }
                }

                diagonal_max_edited(i, val) => {
                    let idx = root.get_flat_index(i, i, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        root.bond_cutoffs[idx].max_distance = val;
                    }
                }

                diagonal_min_edited(i, val) => {
                    let idx = root.get_flat_index(i, i, root.atom_counts.length);
                    if (idx < root.bond_cutoffs.length) {
                        root.bond_cutoffs[idx].min_distance = val;
                    }
                }
            }
        }
    }
}

