// Correlation - Liquid and Amorphous Solid Analysis Tool
// Copyright © 2013-2026 Isaías Rodríguez (isurwars@gmail.com)
// SPDX-License-Identifier: AGPL-3.0-only
// Full license: https://github.com/Isurwars/Correlation/blob/main/LICENSE

import {
    MaterialPalette,
    MaterialStyleMetrics,
    MaterialTypography,
    FilledCard,
    Vertical,
    Horizontal,
    TabItem,
    ScrollView,
    MenuItem,
} from "../material/material.slint";
import { PresetManagerCard } from "PresetManagerCard.slint";
import { DynamicPropertiesCard } from "DynamicPropertiesCard.slint";
import { BondCutoffsCard } from "BondCutoffsCard.slint";
import { RDFOptionsCard } from "RDFOptionsCard.slint";
import { BondAngleOptionsCard } from "BondAngleOptionsCard.slint";
import { SQOptionsCard } from "SQOptionsCard.slint";
import { RingsOptionsCard } from "RingsOptionsCard.slint";
import { SmoothingOptionsCard } from "SmoothingOptionsCard.slint";
import { AdvancedOptionsCard } from "AdvancedOptionsCard.slint";
import { AnalysesSelectionCard } from "AnalysesSelectionCard.slint";
import { ExportSettingsCard } from "../preview/ExportSettingsCard.slint";
import {
    AtomCount,
    BondCutoff,
    AnalysisOptions,
    AppErrors,
    CalculatorGroup,
} from "../Types.slint";

export component OptionsCard inherits FilledCard {
    in property <bool> enabled: true;
    in property <bool> file_loaded: false;
    in property <bool> has_radial_active: false;
    in property <bool> has_angular_active: false;
    in property <bool> has_scattering_active: false;
    in property <bool> has_rings_active: false;

    in property <[CalculatorGroup]> calculator_groups: [];
    in property <int> total_calculator_count: 0;
    callback toggle_calculator(string, bool);

    in-out property <int> material_type: 0;
    in-out property <[BondCutoff]> bond_cutoffs: [];
    in property <[AtomCount]> atom_counts: [];

    in property <int> num_frames: 0;

    in-out property <AnalysisOptions> analysis_options;
    in-out property <AppErrors> app_errors;

    property <string> time_step <=> analysis_options.time_step;
    property <string> min_frame <=> analysis_options.min_frame;
    property <string> max_frame <=> analysis_options.max_frame;

    property <string> r_max <=> analysis_options.r_max;
    property <string> r_bin_width <=> analysis_options.r_bin_width;
    property <string> angle_bin_width <=> analysis_options.angle_bin_width;
    property <string> dihedral_bin_width <=> analysis_options.dihedral_bin_width;
    property <string> q_max <=> analysis_options.q_max;
    property <string> q_bin_width <=> analysis_options.q_bin_width;
    property <string> r_int_max <=> analysis_options.r_int_max;
    property <string> max_ring_size <=> analysis_options.max_ring_size;
    property <bool> smoothing <=> analysis_options.smoothing_enabled;
    property <string> smoothing_sigma <=> analysis_options.smoothing_sigma;
    property <int> smoothing_kernel <=> analysis_options.smoothing_kernel;
    property <string> lef_cutoff <=> analysis_options.lef_cutoff;
    property <string> lef_sigma <=> analysis_options.lef_sigma;
    property <string> hyper_samples <=> analysis_options.hyper_samples;

    property <string> time_step_error <=> app_errors.time_step_error;
    property <string> min_frame_error <=> app_errors.min_frame_error;
    property <string> max_frame_error <=> app_errors.max_frame_error;

    property <string> r_max_error <=> app_errors.r_max_error;
    property <string> r_bin_error <=> app_errors.r_bin_error;
    property <string> angle_bin_error <=> app_errors.angle_bin_error;
    property <string> dihedral_bin_error <=> app_errors.dihedral_bin_error;
    property <string> q_max_error <=> app_errors.q_max_error;
    property <string> q_bin_error <=> app_errors.q_bin_error;
    property <string> r_int_max_error <=> app_errors.r_int_max_error;
    property <string> max_ring_error <=> app_errors.max_ring_error;
    property <string> smoothing_sigma_error <=> app_errors.smoothing_sigma_error;
    property <string> lef_cutoff_error <=> app_errors.lef_cutoff_error;
    property <string> lef_sigma_error <=> app_errors.lef_sigma_error;
    property <string> hyper_samples_error <=> app_errors.hyper_samples_error;

    // Dynamic Properties
    in property <string> diff_msd: "";
    in property <string> diff_vacf: "";
    in property <string> relaxation_time: "";
    in property <string> deborah_number: "";

    // Export Settings
    in-out property <int> export_size_preset: 0;
    in-out property <int> export_palette: 0;
    in-out property <string> export_font_scale: "1.0";
    in-out property <string> export_line_width: "3.0";
    in-out property <bool> export_show_legend: true;
    in-out property <bool> export_show_grid: true;
    in-out property <bool> export_show_markers: false;
    in-out property <bool> export_fill_area: false;

    in property <string> export_font_scale_error: "";
    in property <string> export_line_width_error: "";

    in-out property <int> active_tab: 0;

    // Preset management
    in property <[MenuItem]> preset_items: [];
    in-out property <int> selected_preset: -1;
    callback load_preset(int);
    callback save_preset(string);
    callback delete_preset(int);

    out property <string> hovered_tooltip_text: 
        preset_manager.hovered_tooltip_text != "" ? preset_manager.hovered_tooltip_text :
        (root.active_tab == 3 && analyses_card.hovered_calculator_description != "") ? analyses_card.hovered_calculator_description :
        (root.active_tab == 2 && export_settings.hovered_tooltip_text != "") ? export_settings.hovered_tooltip_text :
        (root.active_tab == 1 && sq_card.hovered_tooltip_text != "") ? sq_card.hovered_tooltip_text :
        (root.active_tab == 1 && rings_card.hovered_tooltip_text != "") ? rings_card.hovered_tooltip_text :
        (root.active_tab == 1 && smoothing_card.hovered_tooltip_text != "") ? smoothing_card.hovered_tooltip_text :
        (root.active_tab == 1 && advanced_card.hovered_tooltip_text != "") ? advanced_card.hovered_tooltip_text :
        (root.active_tab == 0 && rdf_card.hovered_tooltip_text != "") ? rdf_card.hovered_tooltip_text :
        (root.active_tab == 0 && angle_card.hovered_tooltip_text != "") ? angle_card.hovered_tooltip_text : "";

    out property <length> hovered_x:
        preset_manager.hovered_tooltip_text != "" ? preset_manager.hovered_x :
        (root.active_tab == 3 && analyses_card.hovered_calculator_description != "") ? analyses_card.hovered_x :
        (root.active_tab == 2 && export_settings.hovered_tooltip_text != "") ? export_settings.hovered_x :
        (root.active_tab == 1 && sq_card.hovered_tooltip_text != "") ? sq_card.hovered_x :
        (root.active_tab == 1 && rings_card.hovered_tooltip_text != "") ? rings_card.hovered_x :
        (root.active_tab == 1 && smoothing_card.hovered_tooltip_text != "") ? smoothing_card.hovered_x :
        (root.active_tab == 1 && advanced_card.hovered_tooltip_text != "") ? advanced_card.hovered_x :
        (root.active_tab == 0 && rdf_card.hovered_tooltip_text != "") ? rdf_card.hovered_x :
        (root.active_tab == 0 && angle_card.hovered_tooltip_text != "") ? angle_card.hovered_x : 0px;

    out property <length> hovered_y:
        preset_manager.hovered_tooltip_text != "" ? preset_manager.hovered_y :
        (root.active_tab == 3 && analyses_card.hovered_calculator_description != "") ? analyses_card.hovered_y :
        (root.active_tab == 2 && export_settings.hovered_tooltip_text != "") ? export_settings.hovered_y :
        (root.active_tab == 1 && sq_card.hovered_tooltip_text != "") ? sq_card.hovered_y :
        (root.active_tab == 1 && rings_card.hovered_tooltip_text != "") ? rings_card.hovered_y :
        (root.active_tab == 1 && smoothing_card.hovered_tooltip_text != "") ? smoothing_card.hovered_y :
        (root.active_tab == 1 && advanced_card.hovered_tooltip_text != "") ? advanced_card.hovered_y :
        (root.active_tab == 0 && rdf_card.hovered_tooltip_text != "") ? rdf_card.hovered_y :
        (root.active_tab == 0 && angle_card.hovered_tooltip_text != "") ? angle_card.hovered_y : 0px;

    out property <length> hovered_width:
        preset_manager.hovered_tooltip_text != "" ? preset_manager.hovered_width :
        (root.active_tab == 3 && analyses_card.hovered_calculator_description != "") ? analyses_card.hovered_width :
        (root.active_tab == 2 && export_settings.hovered_tooltip_text != "") ? export_settings.hovered_width :
        (root.active_tab == 1 && sq_card.hovered_tooltip_text != "") ? sq_card.hovered_width :
        (root.active_tab == 1 && rings_card.hovered_tooltip_text != "") ? rings_card.hovered_width :
        (root.active_tab == 1 && smoothing_card.hovered_tooltip_text != "") ? smoothing_card.hovered_width :
        (root.active_tab == 1 && advanced_card.hovered_tooltip_text != "") ? advanced_card.hovered_width :
        (root.active_tab == 0 && rdf_card.hovered_tooltip_text != "") ? rdf_card.hovered_width :
        (root.active_tab == 0 && angle_card.hovered_tooltip_text != "") ? angle_card.hovered_width : 0px;

    out property <length> hovered_height:
        preset_manager.hovered_tooltip_text != "" ? preset_manager.hovered_height :
        (root.active_tab == 3 && analyses_card.hovered_calculator_description != "") ? analyses_card.hovered_height :
        (root.active_tab == 2 && export_settings.hovered_tooltip_text != "") ? export_settings.hovered_height :
        (root.active_tab == 1 && sq_card.hovered_tooltip_text != "") ? sq_card.hovered_height :
        (root.active_tab == 1 && rings_card.hovered_tooltip_text != "") ? rings_card.hovered_height :
        (root.active_tab == 1 && smoothing_card.hovered_tooltip_text != "") ? smoothing_card.hovered_height :
        (root.active_tab == 1 && advanced_card.hovered_tooltip_text != "") ? advanced_card.hovered_height :
        (root.active_tab == 0 && rdf_card.hovered_tooltip_text != "") ? rdf_card.hovered_height :
        (root.active_tab == 0 && angle_card.hovered_tooltip_text != "") ? angle_card.hovered_height : 0px;

    Vertical {
        width: 100%;
        height: 100%;
        padding: 0;

        // Dynamic Physical Properties (always visible above options when calculated)
        DynamicPropertiesCard {
            diff_msd: root.diff_msd;
            diff_vacf: root.diff_vacf;
            relaxation_time: root.relaxation_time;
            deborah_number: root.deborah_number;
        }

        // Preset Manager (collapsible, always visible above tabs)
        preset_manager := PresetManagerCard {
            enabled: root.file_loaded;
            preset_items: root.preset_items;
            selected_preset <=> root.selected_preset;
            load_preset(idx) => { root.load_preset(idx); }
            save_preset(name) => { root.save_preset(name); }
            delete_preset(idx) => { root.delete_preset(idx); }
        }

        // TAB NAVIGATION BAR (4 tabs: Common, Advanced, Plot, Analyses)
        Horizontal {
            height: MaterialStyleMetrics.size_40;
            spacing: MaterialStyleMetrics.spacing_4;
            padding-bottom: MaterialStyleMetrics.spacing_4;

            TabItem {
                text: "Common";
                active: root.active_tab == 0;
                clicked => { root.active_tab = 0; }
            }

            TabItem {
                text: "Advanced";
                active: root.active_tab == 1;
                clicked => { root.active_tab = 1; }
            }

            TabItem {
                text: "Plot";
                active: root.active_tab == 2;
                clicked => { root.active_tab = 2; }
            }

            TabItem {
                text: "Analyses";
                active: root.active_tab == 3;
                clicked => { root.active_tab = 3; }
            }
        }

        ScrollView {
            viewport-height: options_layout.preferred-height;
            options_layout := Vertical {
                padding: 0;
                spacing: MaterialStyleMetrics.spacing_4;

                // Common Tab Options (Bond Cutoffs, RDF, Angular)
                bond_cutoffs_card := BondCutoffsCard {
                    file_loaded: root.file_loaded;
                    bond_cutoffs <=> root.bond_cutoffs;
                    atom_counts: root.atom_counts;
                    visible: root.active_tab == 0;
                    height: root.active_tab == 0 ? self.preferred-height : 0px;
                }

                rdf_card := RDFOptionsCard {
                    file_loaded: root.file_loaded;
                    r_max <=> root.r_max;
                    r_bin_width <=> root.r_bin_width;
                    r_max_error: root.r_max_error;
                    r_bin_error: root.r_bin_error;
                    visible: root.active_tab == 0 && root.has_radial_active;
                    height: (root.active_tab == 0 && root.has_radial_active) ? self.preferred-height : 0px;
                }

                angle_card := BondAngleOptionsCard {
                    file_loaded: root.file_loaded;
                    angle_bin_width <=> root.angle_bin_width;
                    dihedral_bin_width <=> root.dihedral_bin_width;
                    angle_bin_error: root.angle_bin_error;
                    dihedral_bin_error: root.dihedral_bin_error;
                    visible: root.active_tab == 0 && root.has_angular_active;
                    height: (root.active_tab == 0 && root.has_angular_active) ? self.preferred-height : 0px;
                }

                // Advanced Tab Options (SQ, Rings, Smoothing)
                sq_card := SQOptionsCard {
                    file_loaded: root.file_loaded;
                    q_max <=> root.q_max;
                    q_bin_width <=> root.q_bin_width;
                    r_int_max <=> root.r_int_max;
                    q_max_error: root.q_max_error;
                    q_bin_error: root.q_bin_error;
                    r_int_max_error: root.r_int_max_error;
                    visible: root.active_tab == 1 && root.has_scattering_active;
                    height: (root.active_tab == 1 && root.has_scattering_active) ? self.preferred-height : 0px;
                }

                rings_card := RingsOptionsCard {
                    file_loaded: root.file_loaded;
                    max_ring_size <=> root.max_ring_size;
                    max_ring_error: root.max_ring_error;
                    visible: root.active_tab == 1 && root.has_rings_active;
                    height: (root.active_tab == 1 && root.has_rings_active) ? self.preferred-height : 0px;
                }

                smoothing_card := SmoothingOptionsCard {
                    file_loaded: root.file_loaded;
                    smoothing_enabled <=> root.smoothing;
                    smoothing_sigma <=> root.smoothing_sigma;
                    smoothing_kernel <=> root.smoothing_kernel;
                    smoothing_sigma_error: root.smoothing_sigma_error;
                    visible: root.active_tab == 1;
                    height: root.active_tab == 1 ? self.preferred-height : 0px;
                }

                advanced_card := AdvancedOptionsCard {
                    file_loaded: root.file_loaded;
                    lef_cutoff <=> root.lef_cutoff;
                    lef_sigma <=> root.lef_sigma;
                    lef_cutoff_error: root.lef_cutoff_error;
                    lef_sigma_error: root.lef_sigma_error;
                    hyper_samples <=> root.hyper_samples;
                    hyper_samples_error: root.hyper_samples_error;
                    visible: root.active_tab == 1;
                    height: root.active_tab == 1 ? self.preferred-height : 0px;
                }

                // Plot Tab Options (Publication Export Settings)
                export_settings := ExportSettingsCard {
                    visible: root.active_tab == 2;
                    height: root.active_tab == 2 ? self.preferred-height : 0px;
                    enabled: root.file_loaded;
                    export_size_preset <=> root.export_size_preset;
                    export_palette <=> root.export_palette;
                    export_font_scale <=> root.export_font_scale;
                    export_line_width <=> root.export_line_width;
                    export_show_legend <=> root.export_show_legend;
                    export_show_grid <=> root.export_show_grid;
                    export_show_markers <=> root.export_show_markers;
                    export_fill_area <=> root.export_fill_area;
                    export_font_scale_error: root.export_font_scale_error;
                    export_line_width_error: root.export_line_width_error;
                }

                // Analyses Tab Options
                analyses_card := AnalysesSelectionCard {
                    file_loaded: root.file_loaded;
                    calculator_groups: root.calculator_groups;
                    total_calculator_count: root.total_calculator_count;
                    toggle_calculator(id, enabled) => {
                        root.toggle_calculator(id, enabled);
                    }
                    visible: root.active_tab == 3;
                    height: root.active_tab == 3 ? self.preferred-height : 0px;
                }
            }
        }
    }
}
